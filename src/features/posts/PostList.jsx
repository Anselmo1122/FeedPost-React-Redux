import { useSelector } from "react-redux";

const PostList = () => {

  const select = useSelector((state)=> state)
  console.log(select)

  return (
    <section>
      
    </section>
  )
};

export default PostList;
